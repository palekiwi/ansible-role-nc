---
apiVersion: v1
kind: Pod
metadata:
  labels:
    app: postgres
  name: postgres
spec:
  containers:
    - name: postgres
      image: "docker.io/library/postgres:16.1"
      env:
        - name: POSTGRES_USER
          value: "{{ nc_db_user }}"
        - name: POSTGRES_PASSWORD
          value: "{{ nc_db_password }}"
        - name: POSTGRES_DB
          value: "{{ nc_db_database }}"
        - name: TZ
          value: Asia/Taipei
  restartPolicy: OnFailure
